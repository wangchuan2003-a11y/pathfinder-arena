# 更新记录

## 2.1.0

- 新增完整中英切换，保留当前地图、历史与回放；记住语言、缩放和速度偏好。
- 新增恢复进入页面前草稿、清除已保存草稿；清除后当前地图仍可导出，偏好设置不受影响。
- 精确编辑后保持选中标记，手动单步播报两种算法的当前格与 g/h/f 数值。

## 2.0.1

- 修复焦点位于地图格子时工具数字键、P 播放/暂停、N 单步失效的问题；新增完整键盘流程回归。

## 2.0.0

### 加权寻路与逐步解释

- 新增沙地（进入成本 5）与水域（进入成本 9），普通格成本为 1。两种算法比较最低总代价，路径步数单独显示。
- 新增 current、frontier 和 `g / h / f` 步骤记录与可视化，支持进度回放，保留暂停、单步和倍速。
- 新增三个教学场景：代价更低的绕路、直觉被墙挡住、真的无路可走。

### 地图编辑与保存

- 扩展撤销并加入重做，按地图快照记录操作；连续绘图的一笔作为一次操作。
- 新增触屏浏览/绘图模式、地图放大、精确方向按钮与应用工具，以及浏览器支持时的全屏。
- 新增当前浏览器的自动草稿、JSON 导入/导出与当前双板 PNG 导出。
- 加权地图使用 v2 分享链接，继续读取 v1；无加权地形的地图保持 v1 编码。JSON 数据格式版本为 1，与产品版本独立。

### 文档与边界

- 补充加权最优性、步骤显示和触屏操作的[一手参考](docs/REFERENCES.md)。
- 明确地图与草稿在浏览器本地处理，分享不包含播放位置和编辑历史。
- 对比指标仍为节点探索与路径成本，不是墙钟性能跑分。本记录不替代测试结果或部署状态。

## 1.0.0

- 初始双网格 A\* / Dijkstra 寻路对比：四向等成本地图、带种子预设、墙与端点编辑。
- 提供探索过程、路径显示、暂停/单步/倍速、地图链接和 GitHub Pages 发布流程。
